import type { CreateBookingServiceData } from '../../types/booking-services';

// Test booking service data for seeding
export const defaultBookingServiceData: CreateBookingServiceData = {
  booking_id: "placeholder-booking-id", // Will be replaced with actual booking_id
  service_id: "placeholder-service-id"  // Will be replaced with actual service_id
};
