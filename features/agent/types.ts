export interface TokenSpent {
  inputTokens: number,
  outputTokens: number,
  cachedTokens: number,
  uncachedTokens: number,
  audioInputTokens: number,
  audioOutputTokens: number,
  totalCost: number
};
